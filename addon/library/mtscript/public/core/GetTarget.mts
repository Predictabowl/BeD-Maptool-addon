[h: dist = json.get(macro.args,0)]
[h: filter = "ALL"]
[h: num = json.length(macro.args)]
[h, if(num > 1): filter = json.get(macro.args,1)]
[h, if(num > 2): switchToken(json.get(macro.args,2))]
[h: source = getImpersonated()]
[h: target = getSelected()]
[h: check = getDistance(source,1,target)]
[h: assert(check<=dist,"Bersaglio fuori portata")]
[h: macro.return = getName(target)]